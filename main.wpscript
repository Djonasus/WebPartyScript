# Commentary
import Tools

Page.title = "Test"
Page.side = "client" #or backend

print("simple text")

a = 0
b = "1"

btn = Button("hello").draw()

fun hello() {
    print("Hello")
}

class RedButton : Button{
    fun _init(text) {
        this.caption = text
    }
}

container Tools.btContainer {
    paragraph("piska").draw()
}
